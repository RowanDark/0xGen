# {{.PluginName}}

Generated by `oxg-plugin init --lang node`.

The scaffold includes placeholders for broker-driven filesystem,
secrets, and network helpers. Adjust `src/capabilities.js` to keep the
manifest, runtime config, and tests aligned with the capabilities you
actually require.
