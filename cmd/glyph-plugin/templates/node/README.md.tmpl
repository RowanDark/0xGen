# {{.PluginName}}

Generated by `glyph-plugin init --lang node`.

The scaffold includes placeholders for broker-driven filesystem and
network helpers. Implementations must rely on the host-provided SDK to
remain sandbox compliant.
