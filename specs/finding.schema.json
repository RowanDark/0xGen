{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/RowanDark/0xgen/specs/finding.schema.json",
  "title": "0xgen Finding v0.2",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "id", "plugin", "type", "message", "severity", "ts"],
  "properties": {
    "version": {
      "type": "string",
      "const": "0.2",
      "description": "Schema version marker."
    },
    "id": {
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "Stable ULID identifier for the finding."
    },
    "plugin": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the plugin that produced the finding."
    },
    "type": {
      "type": "string",
      "minLength": 1,
      "description": "Machine readable finding type."
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "description": "Human friendly message describing the finding."
    },
    "target": {
      "type": "string",
      "minLength": 1,
      "description": "Target asset associated with the finding."
    },
    "evidence": {
      "type": "string",
      "minLength": 1,
      "description": "Optional evidence string from the plugin."
    },
    "severity": {
      "type": "string",
      "enum": ["info", "low", "med", "high", "crit"],
      "description": "Severity level assigned to the finding."
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 timestamp indicating when the finding was observed."
    },
    "meta": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Arbitrary plugin supplied key/value metadata."
    }
  }
}
